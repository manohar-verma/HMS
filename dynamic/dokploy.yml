http:
  routers:
    acme-http:
      rule: PathPrefix(`/.well-known/acme-challenge/`)
      entryPoints:
        - web
      service: acme-service
      priority: 100

  services:
    acme-service:
      loadBalancer:
        servers:
          - url: http://127.0.0.1